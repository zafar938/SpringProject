

CREATE TABLE "SCOTT"."LAYERED_CUSTOMER" 
   (	"CNO" NUMBER(*,0) NOT NULL ENABLE, 
	"CNAME" VARCHAR2(20 BYTE), 
	"CADD" VARCHAR2(20 BYTE), 
	"PAMT" FLOAT(10), 
	"RATE" FLOAT(10), 
	"TIME" FLOAT(10), 
	"INTRANT" FLOAT(10), 
	 CONSTRAINT "LAYERED_CUSTOMER_PK" PRIMARY KEY ("CNO")
	 
============================================================================

   CREATE SEQUENCE  "SCOTT"."SEQ_CNO"  MINVALUE 10 MAXVALUE 5000 INCREMENT BY 1 START WITH 10 CACHE 20 NOORDER  NOCYCLE ;